{
  "name": "sentinel-sdd",
  "version": "0.1.0",
  "private": true,
  "description": "DevSecOps Sentinel AI — spec-first monorepo (MFE shell/config/metrics + Express API + AI Agent Engine)",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build": "turbo run build"
  }
}
